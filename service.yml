apiVersion: v1
kind: Service
metadata:
  name: captive-portal-service
  namespace: default
spec:
  selector:
    app: captive-portal
  type: ClusterIP
  sessionAffinity: None
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 10800
  ports:
  - name: captive-portal-service-port
    protocol: TCP
    port: 80
    targetPort: 80
